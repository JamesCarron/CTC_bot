[pytest]
# Put the project root on sys.path so `pytest` works invoked directly, not just
# via `python -m pytest` (which adds the cwd implicitly). Pixi tasks call the
# pytest executable, so without this the ctc_bot package is not importable.
pythonpath = .
testpaths = tests
