requests>=2.31
jinja2>=3.1
python-dateutil>=2.9

# TLS: verify against the Windows certificate store. This machine sits behind
# something that inspects TLS and presents its own root CA, which certifi does
# not carry - without this, every HTTPS call fails CERTIFICATE_VERIFY_FAILED.
truststore>=0.9

# Encrypts the stored RaceClocker login with Windows DPAPI
pywin32>=306; sys_platform == "win32"

# matplotlib was here for a PNG renderer that was never built. Left out to keep
# the container image small; add it back when that lands.

# Dev
pytest>=8.0
